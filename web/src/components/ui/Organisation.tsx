import { Organisation as Org } from "@/types/organisation";
import Link from "next/link";
import { FC } from "react";
import { Logo } from "./Logo";

export const Organisation: FC<Org> = ({ id, title }) => {
  return <></>;
};
